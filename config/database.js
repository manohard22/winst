// Centralized database configuration for all services
const dbConfig = {
  host: "localhost",
  port: 5432,
  database: "winstdb",
  user: "winst_db_user",
  password: "root"
};

module.exports = dbConfig;
